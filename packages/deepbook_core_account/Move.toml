[package]
name = "deepbook_core_account"
edition = "2024.beta"
version = "0.0.1"

[dependencies]
account = { local = "../account" }
deepbook = { local = "../deepbook" }
# Keep this direct dependency on the release revision resolved through `deepbook`;
# the override prevents divergent token copies from entering this package graph.
token = { git = "https://github.com/MystenLabs/deepbookv3.git", subdir = "packages/token", rev = "75d560614b96f8ded0f511000d65e957c3ec6b1a", override = true }
